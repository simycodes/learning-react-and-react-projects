import React from 'react';

const fetchErrorMessage = () => {
  return (
    <div className="loading">
      <h1>Loading Failed, Try Again Later...</h1>
    </div>
  );
};

export default fetchErrorMessage;
